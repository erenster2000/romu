{
  "name": "@romu/core",
  "version": "0.1.0",
  "description": "Romu's engine: build pipeline, dev server, asset pipeline, adapter runner",
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/erenster2000/romu.git",
    "directory": "packages/core"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@romu/specs": "workspace:*",
    "esbuild": "^0.28.0",
    "sharp": "^0.34.0",
    "vite": "^7.0.0",
    "zod": "^4.0.0"
  }
}
